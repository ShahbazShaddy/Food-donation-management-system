<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">
      <span class="text-primary">Food</span><span class="text-success">Aid</span>
    </a>
    
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" 
            data-bs-target="#navbarNav" aria-controls="navbarNav" 
            aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link px-3 <%= typeof page !== 'undefined' && page === 'welcome' ? 'active fw-semibold' : '' %>" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3 <%= typeof page !== 'undefined' && page === 'mission' ? 'active fw-semibold' : '' %>" href="/home/mission">Our Mission</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3 <%= typeof page !== 'undefined' && page === 'contact' ? 'active fw-semibold' : '' %>" href="/home/contactUs">Contact Us</a>
        </li>
        <% if(!currentUser) { %>
          <li class="nav-item">
            <a class="nav-link px-3 <%= typeof page !== 'undefined' && page === 'login' ? 'active fw-semibold' : '' %>" href="/auth/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-primary rounded-pill px-4 ms-2" href="/auth/signup">Sign Up</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link px-3" href="/auth/logout">Logout</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-success rounded-pill px-4 ms-2" href="/<%= currentUser.role %>/dashboard">Dashboard</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<!-- Add this CSS to your style.css file or inline for transition effects -->
<style>
  .navbar {
    transition: all 0.3s ease;
  }
  
  .navbar-brand {
    font-size: 1.5rem;
  }
  
  .nav-link {
    position: relative;
    transition: all 0.2s ease;
  }
  
  .nav-link:hover {
    color: #0d6efd !important;
  }
  
  .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 50%;
    transform: translateX(-50%);
    width: 20px;
    height: 2px;
    background-color: #0d6efd;
  }
  
  @media (max-width: 991px) {
    .navbar-nav {
      padding: 1rem 0;
    }
    
    .nav-item {
      margin: 0.5rem 0;
    }
  }
</style>